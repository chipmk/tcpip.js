{
  "name": "node-example",
  "private": true,
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc -b",
    "build:watch": "tsc -b --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "tcpip": "^0.1.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
