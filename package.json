{
  "name": "tcpip-lwip",
  "version": "0.1.0",
  "description": "TCP/IP stack in the browser",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "test": "vitest",
    "clean": "rm -rf dist",
    "make": "CC='docker compose run --rm wasi-sdk /opt/wasi-sdk/bin/clang' AR='docker compose run --rm wasi-sdk /opt/wasi-sdk/bin/ar' make"
  },
  "files": [
    "dist/**/*"
  ],
  "dependencies": {
    "@bjorn3/browser_wasi_shim": "^0.3.0"
  },
  "devDependencies": {
    "@total-typescript/tsconfig": "^1.0.4",
    "@types/node": "^22.8.6",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
